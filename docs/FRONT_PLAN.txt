Отлично! Теперь у меня есть полная картина проекта. Давайте составим подробный план для фронтендеров по двум блокам функций.

---

# 📋 Детальный план реализации для фронтендеров

## 🎨 Блок 1: Улучшение UX/UI

### 1.1 Онбординг-туры с подсказками для новых пользователей

**Цель:** Помочь новым пользователям быстро освоиться в приложении через интерактивные туры.

**Технологии:**
- Библиотека: `react-joyride` или `intro.js-react` или `driver.js`
- Альтернатива: Собственная реализация на основе Tooltip компонентов

**Что нужно реализовать:**

#### 1.1.1 Компонент OnboardingTour
```typescript
// src/components/OnboardingTour.tsx
- Обертка для библиотеки туров
- Управление состоянием тура (текущий шаг, завершен ли)
- Сохранение прогресса в localStorage
- Возможность пропустить/перезапустить тур
```

#### 1.1.2 Туры для разных страниц
- **Dashboard Tour**: Объяснение статистики, календаря, создания активностей
- **Map Tour**: Как искать события, фильтровать, добавлять в план
- **Social Tour**: Как добавлять друзей, создавать группы, общаться
- **Profile Tour**: Настройки, достижения, приоритеты

#### 1.1.3 Хранилище состояния
```typescript
// src/store/onboardingStore.ts
- Zustand store для управления состоянием онбординга
- Флаги: completedTours, currentTour, showTour
- Методы: startTour, completeTour, skipTour, resetTours
```

#### 1.1.4 Интеграция
- Добавить кнопку "?" в Navigation для повторного запуска туров
- Автоматический запуск при первом входе пользователя
- Проверка флага `user.is_first_login` из API

**Дизайн подсказок:**
- Темная полупрозрачная подложка (backdrop)
- Яркие акцентные цвета для выделения элементов
- Анимации появления/исчезания
- Прогресс-бар (шаг X из Y)
- Кнопки: "Далее", "Назад", "Пропустить", "Завершить"

---

### 1.2 Анимации и микроинтерактивность

**Цель:** Сделать интерфейс живым и отзывчивым, улучшить восприятие действий пользователя.

**Технологии:**
- `framer-motion` - для сложных анимаций
- CSS transitions/animations - для простых эффектов
- `react-spring` - альтернатива для физических анимаций

**Что нужно реализовать:**

#### 1.2.1 Установка библиотек
```bash
npm install framer-motion
npm install @react-spring/web  # опционально
```

#### 1.2.2 Анимации для карточек активностей
```typescript
// src/components/ActivityCard.tsx
- Hover эффекты: подъем карточки, изменение тени
- Анимация появления при загрузке (stagger effect)
- Анимация удаления (fade out + slide)
- Анимация завершения (галочка с конфетти)
- Пульсация для срочных задач
```

#### 1.2.3 Анимации для модальных окон
```typescript
// Все модальные окна
- Fade in/out для backdrop
- Scale + fade для самого окна
- Slide up для мобильных версий
- Spring анимация для закрытия
```

#### 1.2.4 Микроинтерактивность
```typescript
// Кнопки
- Ripple effect при клике
- Scale на hover
- Loading состояние с спиннером
- Success/Error анимации

// Инпуты
- Focus анимация границы
- Shake при ошибке валидации
- Success галочка при корректном вводе

// Навигация
- Подчеркивание активной страницы (slide effect)
- Иконки с bounce при наведении
- Badge с пульсацией для уведомлений

// Статистика
- Анимация чисел (count up)
- Прогресс-бары с плавным заполнением
- Графики с анимацией отрисовки
```

#### 1.2.5 Переходы между страницами
```typescript
// src/App.tsx
- Page transitions с framer-motion
- Fade + slide для смены страниц
- Shared element transitions (если возможно)
```

#### 1.2.6 Компонент AnimatedNumber
```typescript
// src/components/AnimatedNumber.tsx
- Плавное изменение числовых значений
- Использовать для статистики, баллов, стрика
```

---

### 1.3 Скелетоны загрузки вместо спиннеров

**Цель:** Показать структуру контента во время загрузки, улучшить воспринимаемую производительность.

**Что нужно реализовать:**

#### 1.3.1 Базовый компонент Skeleton
```typescript
// src/components/Skeleton.tsx
interface SkeletonProps {
  variant: 'text' | 'circular' | 'rectangular' | 'rounded';
  width?: string | number;
  height?: string | number;
  className?: string;
}

- Shimmer эффект (градиент движется)
- Поддержка темной темы
- Разные варианты форм
```

#### 1.3.2 Специализированные скелетоны
```typescript
// src/components/skeletons/ActivityCardSkeleton.tsx
- Имитация структуры ActivityCard
- Заголовок, описание, время, кнопки

// src/components/skeletons/StatsCardSkeleton.tsx
- Для карточек статистики на Dashboard

// src/components/skeletons/EventCardSkeleton.tsx
- Для карточек событий на MapPage

// src/components/skeletons/ChatMessageSkeleton.tsx
- Для сообщений в чате

// src/components/skeletons/ProfileSkeleton.tsx
- Для страницы профиля
```

#### 1.3.3 Замена loading состояний
```typescript
// Заменить во всех страницах:
// ❌ Было:
if (loading) return <div>Загрузка...</div>

// ✅ Стало:
if (loading) return <ActivityCardSkeleton count={3} />
```

#### 1.3.4 Список страниц для обновления
- DashboardPage: StatsCardSkeleton + ActivityCardSkeleton
- MapPage: EventCardSkeleton
- FriendsPage: UserCardSkeleton
- GroupsPage: GroupCardSkeleton
- ChatPage: ChatMessageSkeleton
- ProfilePage: ProfileSkeleton
- AnalyticsPage: ChartSkeleton

---

### 1.4 Оптимистичные обновления UI

**Цель:** Мгновенная реакция интерфейса на действия пользователя без ожидания ответа сервера.

**Технологии:**
- TanStack Query (уже установлен) - `useMutation` с `onMutate`
- Zustand для локального состояния

**Что нужно реализовать:**

#### 1.4.1 Настройка TanStack Query
```typescript
// src/services/queryClient.ts
- Создать отдельный файл с настройками QueryClient
- Настроить retry, staleTime, cacheTime
- Добавить глобальные обработчики ошибок
```

#### 1.4.2 Оптимистичные мутации для активностей
```typescript
// src/hooks/useActivities.ts
- useCreateActivity: мгновенно добавить в список
- useUpdateActivity: мгновенно обновить в списке
- useDeleteActivity: мгновенно удалить из списка
- useCompleteActivity: мгновенно отметить как выполненное

// При ошибке - откатить изменения (rollback)
// Показать toast с ошибкой
```

#### 1.4.3 Оптимистичные обновления для социальных функций
```typescript
// src/hooks/useFriends.ts
- useSendFriendRequest: мгновенно показать "Запрос отправлен"
- useAcceptFriendRequest: мгновенно добавить в друзья
- useRemoveFriend: мгновенно удалить из списка

// src/hooks/useGroups.ts
- useJoinGroup: мгновенно показать "Вы участник"
- useLeaveGroup: мгновенно обновить статус
```

#### 1.4.4 Оптимистичные обновления для чатов
```typescript
// src/hooks/useChat.ts
- useSendMessage: мгновенно добавить сообщение в чат
- Показать индикатор "отправляется..."
- При успехе - заменить на реальное сообщение с ID
- При ошибке - показать "не отправлено", кнопку повтора
```

#### 1.4.5 Компонент Toast для уведомлений
```typescript
// src/components/Toast.tsx
- Библиотека: react-hot-toast или sonner
- Уведомления об успехе/ошибке
- Позиция: top-right
- Автозакрытие через 3-5 секунд
```

---

### 1.5 Drag & Drop для планирования активностей

**Цель:** Позволить пользователям перетаскивать активности для изменения времени/порядка.

**Технологии:**
- `@dnd-kit/core` + `@dnd-kit/sortable` (современная, легкая)
- Альтернатива: `react-beautiful-dnd` (более старая, но стабильная)

**Что нужно реализовать:**

#### 1.5.1 Установка библиотек
```bash
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

#### 1.5.2 Компонент DraggableActivityCard
```typescript
// src/components/DraggableActivityCard.tsx
- Обертка над ActivityCard с drag функционалом
- Визуальная обратная связь при перетаскивании
- Призрачное изображение (ghost image)
- Индикатор места вставки (drop indicator)
```

#### 1.5.3 Обновление DashboardPage
```typescript
// src/pages/DashboardPage.tsx
- Обернуть список активностей в DndContext
- Обработчик onDragEnd для пересортировки
- Автоматический пересчет времени при перемещении
- Вызов API для сохранения нового порядка
```

#### 1.5.4 Календарный вид (опционально)
```typescript
// src/components/CalendarView.tsx
- Недельный/дневной вид с временными слотами
- Drag & Drop между днями
- Изменение длительности через resize
- Визуализация конфликтов времени
```

#### 1.5.5 Мобильная версия
- Long press для активации drag на мобильных
- Тактильная обратная связь (vibration API)
- Увеличенные зоны для drop

---

## 🎯 Блок 2: Дополнительные фичи

### 2.1 Челленджи и квесты с наградами

**Цель:** Геймификация через систему челленджей, которые мотивируют пользователей выполнять активности.

**Что нужно реализовать:**

#### 2.1.1 Типы данных
```typescript
// src/types/index.ts
export interface Challenge {
  id: number;
  title: string;
  description: string;
  type: 'daily' | 'weekly' | 'monthly' | 'special';
  category?: PriorityCategory;
  goal: number; // целевое значение
  current_progress: number; // текущий прогресс
  reward_points: number;
  reward_currency?: number; // виртуальная валюта
  start_date: string;
  end_date: string;
  status: 'active' | 'completed' | 'expired';
  icon?: string;
  difficulty: 'easy' | 'medium' | 'hard';
}

export interface Quest {
  id: number;
  title: string;
  description: string;
  steps: QuestStep[];
  reward_points: number;
  reward_currency: number;
  reward_items?: string[]; // бейджи, аватары и т.д.
  status: 'locked' | 'active' | 'completed';
  completion_percentage: number;
}

export interface QuestStep {
  id: number;
  title: string;
  description: string;
  completed: boolean;
  order: number;
}
```

#### 2.1.2 API методы
```typescript
// src/services/api.ts
export const challengesAPI = {
  getChallenges: async (type?: string) => {...},
  getChallenge: async (id: number) => {...},
  acceptChallenge: async (id: number) => {...},
  completeChallenge: async (id: number) => {...},
  getActiveChallenges: async () => {...},
};

export const questsAPI = {
  getQuests: async () => {...},
  getQuest: async (id: number) => {...},
  startQuest: async (id: number) => {...},
  completeQuestStep: async (questId: number, stepId: number) => {...},
};
```

#### 2.1.3 Страница ChallengesPage
```typescript
// src/pages/ChallengesPage.tsx
Структура:
- Навигация (Navigation)
- Табы: "Активные" | "Доступные" | "Завершенные"
- Фильтры: по типу, категории, сложности
- Список челленджей (карточки)
- Модальное окно с деталями челленджа
```

#### 2.1.4 Компонент ChallengeCard
```typescript
// src/components/ChallengeCard.tsx
Элементы:
- Иконка челленджа
- Название и описание
- Прогресс-бар (current/goal)
- Награда (баллы + валюта)
- Таймер до окончания (для временных)
- Кнопка "Принять" / "Продолжить" / "Завершен"
- Индикатор сложности (звездочки)
```

#### 2.1.5 Компонент QuestCard
```typescript
// src/components/QuestCard.tsx
Элементы:
- Обложка квеста
- Название и описание
- Список шагов с чекбоксами
- Общий прогресс (%)
- Награды
- Кнопка "Начать" / "Продолжить"
```

#### 2.1.6 Уведомления о челленджах
```typescript
// Интеграция с Dashboard
- Виджет "Активные челленджи" на главной
- Уведомления при завершении челленджа
- Анимация получения награды (конфетти)
- Предложение новых челленджей на основе активности
```

---

### 2.2 Виртуальная валюта (баллы → скидки/призы)

**Цель:** Монетизация геймификации через внутреннюю валюту, которую можно обменять на реальные бонусы.

**Что нужно реализовать:**

#### 2.2.1 Типы данных
```typescript
// src/types/index.ts
export interface VirtualCurrency {
  user_id: number;
  balance: number; // текущий баланс
  total_earned: number; // всего заработано
  total_spent: number; // всего потрачено
}

export interface CurrencyTransaction {
  id: number;
  user_id: number;
  amount: number; // положительное = заработок, отрицательное = трата
  type: 'earned' | 'spent' | 'bonus' | 'refund';
  source: string; // "challenge_completed", "activity_completed", "store_purchase"
  description: string;
  created_at: string;
}

export interface StoreItem {
  id: number;
  title: string;
  description: string;
  category: 'discount' | 'premium' | 'cosmetic' | 'physical';
  price: number; // в виртуальной валюте
  image_url?: string;
  stock?: number; // количество доступных
  partner?: string; // партнер, предоставляющий приз
  discount_code?: string; // промокод для скидки
  available: boolean;
}

export interface Purchase {
  id: number;
  user_id: number;
  item_id: number;
  item_title: string;
  price: number;
  status: 'pending' | 'completed' | 'used';
  purchased_at: string;
  used_at?: string;
  code?: string; // промокод или код активации
}
```

#### 2.2.2 API методы
```typescript
// src/services/api.ts
export const currencyAPI = {
  getBalance: async () => {...},
  getTransactions: async (limit?: number) => {...},
  getStoreItems: async (category?: string) => {...},
  purchaseItem: async (itemId: number) => {...},
  getPurchases: async () => {...},
  useItem: async (purchaseId: number) => {...},
};
```

#### 2.2.3 Компонент CurrencyBalance
```typescript
// src/components/CurrencyBalance.tsx
- Отображение текущего баланса
- Иконка монеты
- Анимация при изменении баланса
- Клик открывает историю транзакций
- Интеграция в Navigation (показывать всегда)
```

#### 2.2.4 Страница StorePage
```typescript
// src/pages/StorePage.tsx
Структура:
- Навигация с балансом
- Категории товаров (табы)
- Сетка карточек товаров
- Фильтры и сортировка
- Модальное окно покупки
```

#### 2.2.5 Компонент StoreItemCard
```typescript
// src/components/StoreItemCard.tsx
Элементы:
- Изображение товара
- Название и описание
- Цена в валюте
- Партнер (если есть)
- Индикатор наличия
- Кнопка "Купить"
- Badge "Новинка" / "Популярное"
```

#### 2.2.6 Страница MyPurchasesPage
```typescript
// src/pages/MyPurchasesPage.tsx
- История покупок
- Активные промокоды
- Использованные призы
- Кнопка "Использовать" для активации
```

#### 2.2.7 Система начисления валюты
```typescript
// Интеграция с существующими функциями
- За выполнение активности: +10-50 монет
- За завершение челленджа: +100-500 монет
- За стрик: +20 монет/день
- За достижения: +50-200 монет
- Бонусы за приглашение друзей: +100 монет
```

---

### 2.3 Блог/Новости о городских событиях (через API)

**Цель:** Информировать пользователей о новостях Санкт-Петербурга, анонсах событий, полезных статьях.

**Что нужно реализовать:**

#### 2.3.1 Типы данных
```typescript
// src/types/index.ts
export interface BlogPost {
  id: number;
  title: string;
  slug: string;
  excerpt: string; // краткое описание
  content: string; // полный текст (HTML или Markdown)
  author: string;
  author_avatar?: string;
  category: 'news' | 'events' | 'guide' | 'tips';
  tags: string[];
  image_url?: string;
  published_at: string;
  views_count: number;
  likes_count: number;
  is_liked?: boolean;
  related_event_id?: number; // связь с событием
}

export interface BlogCategory {
  id: string;
  name: string;
  icon: string;
  count: number;
}
```

#### 2.3.2 API методы
```typescript
// src/services/api.ts
export const blogAPI = {
  getPosts: async (params?: {
    category?: string;
    tag?: string;
    limit?: number;
    offset?: number;
  }) => {...},
  getPost: async (slug: string) => {...},
  likePost: async (id: number) => {...},
  getCategories: async () => {...},
  getTrendingPosts: async (limit?: number) => {...},
  searchPosts: async (query: string) => {...},
};
```

#### 2.3.3 Страница BlogPage
```typescript
// src/pages/BlogPage.tsx
Структура:
- Навигация
- Hero секция с featured постом
- Категории (горизонтальный скролл)
- Сетка постов (3 колонки на десктопе)
- Пагинация или infinite scroll
- Sidebar: популярные посты, теги
```

#### 2.3.4 Компонент BlogPostCard
```typescript
// src/components/BlogPostCard.tsx
Элементы:
- Изображение (cover)
- Категория (badge)
- Заголовок
- Excerpt (2-3 строки)
- Автор и дата
- Счетчики: просмотры, лайки
- Hover эффект с увеличением
```

#### 2.3.5 Страница BlogPostPage
```typescript
// src/pages/BlogPostPage.tsx
Структура:
- Навигация
- Hero с изображением и заголовком
- Метаданные: автор, дата, категория
- Контент (рендеринг HTML/Markdown)
- Кнопка "Нравится"
- Связанное событие (если есть)
- Похожие посты
- Кнопка "Поделиться"
```

#### 2.3.6 Интеграция с Dashboard
```typescript
// Виджет "Новости" на главной
- Показывать 2-3 последних поста
- Компактный вид
- Ссылка "Все новости"
```

#### 2.3.7 Markdown рендеринг
```bash
npm install react-markdown remark-gfm rehype-raw
```
```typescript
// src/components/MarkdownRenderer.tsx
- Рендеринг Markdown в HTML
- Поддержка изображений, ссылок, кода
- Стилизация под дизайн приложения
```

---

### 2.4 Поиск попутчиков на мероприятия

**Цель:** Помочь пользователям находить компанию для посещения событий, создавать группы по интересам.

**Что нужно реализовать:**

#### 2.4.1 Типы данных
```typescript
// src/types/index.ts
export interface EventCompanion {
  id: number;
  event_id: number;
  user_id: number;
  username: string;
  avatar_url?: string;
  message: string; // "Ищу компанию на концерт!"
  max_companions: number; // сколько человек ищет
  current_companions: number; // сколько уже нашлось
  status: 'open' | 'full' | 'closed';
  created_at: string;
  interests?: string[]; // теги интересов
}

export interface CompanionRequest {
  id: number;
  companion_post_id: number;
  user_id: number;
  username: string;
  message: string;
  status: 'pending' | 'accepted' | 'rejected';
  created_at: string;
}

export interface EventGroup {
  id: number;
  event_id: number;
  name: string;
  description: string;
  creator_id: number;
  members: EventGroupMember[];
  max_members: number;
  meeting_point?: string;
  meeting_time?: string;
  status: 'open' | 'full' | 'closed';
}

export interface EventGroupMember {
  user_id: number;
  username: string;
  avatar_url?: string;
  joined_at: string;
}
```

#### 2.4.2 API методы
```typescript
// src/services/api.ts
export const companionsAPI = {
  // Поиск попутчиков
  getCompanionPosts: async (eventId: number) => {...},
  createCompanionPost: async (data: {
    event_id: number;
    message: string;
    max_companions: number;
  }) => {...},
  deleteCompanionPost: async (id: number) => {...},
  
  // Запросы на присоединение
  sendCompanionRequest: async (postId: number, message: string) => {...},
  getMyRequests: async () => {...},
  acceptRequest: async (requestId: number) => {...},
  rejectRequest: async (requestId: number) => {...},
  
  // Группы для событий
  getEventGroups: async (eventId: number) => {...},
  createEventGroup: async (data: {
    event_id: number;
    name: string;
    description: string;
    max_members: number;
  }) => {...},
  joinEventGroup: async (groupId: number) => {...},
  leaveEventGroup: async (groupId: number) => {...},
};
```

#### 2.4.3 Обновление EventCard на MapPage
```typescript
// src/components/EventCard.tsx
Добавить:
- Индикатор "Ищут попутчиков" (количество)
- Кнопка "Найти компанию"
- Открывает модальное окно с попутчиками
```

#### 2.4.4 Модальное окно CompanionsModal
```typescript
// src/components/CompanionsModal.tsx
Структура:
- Информация о событии (краткая)
- Табы: "Ищут компанию" | "Группы" | "Мои объявления"
- Список объявлений о поиске
- Кнопка "Создать объявление"
- Форма создания объявления
```

#### 2.4.5 Компонент CompanionPostCard
```typescript
// src/components/CompanionPostCard.tsx
Элементы:
- Аватар и имя пользователя
- Сообщение
- Счетчик: "2 из 4 мест занято"
- Теги интересов
- Кнопка "Присоединиться"
- Для своих постов: список запросов, кнопки принять/отклонить
```

#### 2.4.6 Компонент EventGroupCard
```typescript
// src/components/EventGroupCard.tsx
Элементы:
- Название группы
- Описание
- Список участников (аватары)
- Место и время встречи
- Счетчик участников
- Кнопка "Вступить" / "Покинуть"
```

#### 2.4.7 Страница MyCompanionsPage
```typescript
// src/pages/MyCompanionsPage.tsx
- Мои объявления о поиске
- Входящие запросы
- Группы, в которых состою
- Предстоящие встречи
```

#### 2.4.8 Уведомления
```typescript
// Интеграция с системой уведомлений
- Новый запрос на присоединение
- Запрос принят/отклонен
- Напоминание о встрече (за 1 час)
- Новое сообщение в группе события
```

---

## 📦 Необходимые npm пакеты

```bash
# Онбординг
npm install react-joyride
# или
npm install driver.js

# Анимации
npm install framer-motion

# Drag & Drop
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

# Уведомления
npm install sonner
# или
npm install react-hot-toast

# Markdown
npm install react-markdown remark-gfm rehype-raw

# Дополнительно
npm install date-fns  # для работы с датами
npm install clsx      # для условных классов
npm install canvas-confetti  # для анимации конфетти при наградах
```

---

## 🗂️ Новая структура файлов

```
frontend/src/
├── components/
│   ├── animations/
│   │   ├── AnimatedNumber.tsx
│   │   ├── ConfettiEffect.tsx
│   │   └── PageTransition.tsx
│   ├── skeletons/
│   │   ├── Skeleton.tsx
│   │   ├── ActivityCardSkeleton.tsx
│   │   ├── StatsCardSkeleton.tsx
│   │   ├── EventCardSkeleton.tsx
│   │   └── ProfileSkeleton.tsx
│   ├── onboarding/
│   │   ├── OnboardingTour.tsx
│   │   ├── TourStep.tsx
│   │   └── TourProgress.tsx
│   ├── challenges/
│   │   ├── ChallengeCard.tsx
│   │